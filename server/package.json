{
  "name": "calendly-clone-server",
  "version": "1.0.0",
  "description": "Backend API for Calendly Clone",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node seed.js",
    "schema": "node -e \"require('./config/db').initializeDatabase()\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "mysql2": "^3.22.0",
    "nodemailer": "^6.10.1",
    "pg": "^8.20.0"
  }
}
